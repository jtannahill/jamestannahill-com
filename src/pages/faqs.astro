---
// Legacy slug. The page now lives at /contact. SSR stub so Astro returns a
// real 301; a config redirect onto a prerendered page gets followed internally
// and served as 200 (same trap as /about).
export const prerender = false;
return Astro.redirect('/contact', 301);
---
